1. Radiobutton, CheckBox, ImageLabel practice

2. Image icon = new ImageIcon("images/user.png").getImage()
   icon.getScaledInstance(150, 150, 0)
   JLabel label = new JLabel(new ImageIcon(icon));
   - image label creation

3. anonymouse class = easy access local variable
   - code readability down..
   write other class = access incomportable
   - reuse possible
   inner class = easy access better than write other class method. but not anonymous class
   half mix..(anonymous class, write other class)
   - reuse possible
