1. AWT(Abstract Window Toolkit), Swing

2. Container
   - Component that can contain other component.
   - can't contain other contain

3. work sequence
   - container object creation
   - layout setting in container
   - component object creation
   - component in container with layout setting
   - keyboard, mouse event handling in component

4. layout
   - BorderLayout (JFrame's default layout)
   - FlowLayout (JPanel's default layout)
   - CardLayout (MouseEvent e.getButton() == 1, left button, 2 = wheel click, 3 = right button)
